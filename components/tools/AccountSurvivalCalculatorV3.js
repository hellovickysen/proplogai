'use client';
import { useMemo, useState } from 'react';
const usd = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 2 });
const n = (v, fallback = 0) => Number.isFinite(Number(v)) ? Number(v) : fallback;
const whole = (a, b) => b > 0 ? Math.floor(a / b) : 0;
function Field({ label, value, setValue, prefix, suffix, step = 1, min = 0.01, max = 100000000, note }) { return <label className="block"><span className="mb-2 block font-mono text-xs uppercase tracking-wider text-white/55">{label}</span><div className="flex items-center rounded-xl border border-white/10 bg-black/20 px-3 focus-within:border-cyan-300/50">{prefix && <span className="mr-1 text-white/45">{prefix}</span>}<input type="number" inputMode="decimal" min={min} max={max} step={step} value={value} onChange={e => setValue(e.target.value)} onBlur={() => setValue(String(Math.min(Math.max(n(value, min), min), max)))} className="w-full bg-transparent py-3 text-base font-semibold text-white outline-none" />{suffix && <span className="ml-2 text-white/45">{suffix}</span>}</div>{note && <span className="mt-1.5 block text-xs text-white/45">{note}</span>}</label>; }
function Metric({ label, value, tone = 'text-white' }) { return <div className="rounded-xl border border-white/10 bg-black/20 p-3"><p className="font-mono text-[10px] uppercase tracking-wider text-white/45">{label}</p><p className={`mt-1 text-lg font-bold ${tone}`}>{value}</p></div>; }
export default function AccountSurvivalCalculatorV3() {
 const [size,setSize]=useState('50000'),[ddMode,setDdMode]=useState('dollar'),[ddDollar,setDdDollar]=useState('2000'),[ddPercent,setDdPercent]=useState('4'),[mode,setMode]=useState('risk'),[basis,setBasis]=useState('day'),[riskMode,setRiskMode]=useState('dollar'),[riskDollar,setRiskDollar]=useState('100'),[riskPercent,setRiskPercent]=useState('0.5'),[trades,setTrades]=useState('2'),[target,setTarget]=useState('20');
 const calc=useMemo(()=>{const account=Math.max(n(size),0.01);const drawdown=Math.min(ddMode==='percent'?account*n(ddPercent)/100:n(ddDollar),account*100);const riskRaw=riskMode==='percent'?account*n(riskPercent)/100:n(riskDollar);const risk=Math.min(Math.max(riskRaw,0),drawdown);const t=Math.max(1,Math.floor(n(trades,1)));const goal=Math.max(1,Math.floor(n(target,1)));const daily=basis==='trade'?Math.min(risk*t,drawdown):risk;const losses=basis==='trade'?whole(drawdown,risk):0;const sessions=basis==='trade'?whole(losses,t):whole(drawdown,daily);const recommendedDaily=drawdown/goal;const recommendedTrade=recommendedDaily/t;return {account,drawdown,risk,daily,t,losses,sessions,goal,recommendedDaily,recommendedTrade};},[size,ddMode,ddDollar,ddPercent,riskMode,riskDollar,riskPercent,trades,target,basis]);
 const setRiskFrom=(v)=>riskMode==='percent'?setRiskPercent(v):setRiskDollar(v);
 const drawdownPct=calc.drawdown/calc.account*100; const dailyPct=calc.daily/calc.account*100;
 return <div className="mx-auto max-w-4xl space-y-5 pb-12"><section className="text-center"><div className="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-2xl border border-cyan-300/20 bg-cyan-300/5 text-3xl">🛡</div><h1 className="font-display text-3xl font-bold text-white md:text-4xl">Account Survival Calculator</h1><p className="mx-auto mt-3 max-w-xl text-sm text-white/55">Calculate a losing-streak buffer from your risk, or work backward from the sessions you want to survive.</p></section>
 <section className="rounded-2xl border border-white/10 bg-white/[0.03] p-5 md:p-6"><p className="font-mono text-xs uppercase tracking-wider text-cyan-300">01 — Account setup</p><div className="mt-5 grid gap-4 md:grid-cols-2"><Field label="Account size" value={size} setValue={setSize} prefix="$"/><div><div className="mb-2 flex items-center justify-between"><span className="font-mono text-xs uppercase tracking-wider text-white/55">Maximum allowed loss / drawdown</span><div className="flex rounded-lg border border-white/10 p-1"><button onClick={()=>setDdMode('dollar')} className={`rounded px-3 py-1 text-xs ${ddMode==='dollar'?'bg-cyan-300/10 text-cyan-200':'text-white/45'}`}>$</button><button onClick={()=>setDdMode('percent')} className={`rounded px-3 py-1 text-xs ${ddMode==='percent'?'bg-cyan-300/10 text-cyan-200':'text-white/45'}`}>%</button></div></div>{ddMode==='dollar'?<Field label="Maximum drawdown" value={ddDollar} setValue={setDdDollar} prefix="$" max={n(size,1)*100}/>:<Field label="Maximum drawdown" value={ddPercent} setValue={setDdPercent} suffix="%" step={0.01} max={100}/>}<p className="mt-2 text-xs text-white/45">{usd.format(calc.drawdown)} drawdown · {drawdownPct.toFixed(2)}% of account</p></div></div></section>
 <section className="rounded-2xl border border-white/10 bg-white/[0.03] p-5 md:p-6"><p className="font-mono text-xs uppercase tracking-wider text-cyan-300">02 — Calculation mode</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><button onClick={()=>setMode('risk')} className={`rounded-xl border p-4 text-left ${mode==='risk'?'border-cyan-300/50 bg-cyan-300/[0.06]':'border-white/10 bg-black/20'}`}><b className="text-white">I know my risk</b><span className="mt-1 block text-xs text-white/45">Calculate losing trades and sessions</span></button><button onClick={()=>setMode('target')} className={`rounded-xl border p-4 text-left ${mode==='target'?'border-cyan-300/50 bg-cyan-300/[0.06]':'border-white/10 bg-black/20'}`}><b className="text-white">I know my survival target</b><span className="mt-1 block text-xs text-white/45">Calculate daily and per-trade risk</span></button></div>{mode==='risk'?<div className="mt-5 space-y-4"><div className="flex gap-2"><button onClick={()=>setBasis('trade')} className={`rounded-lg border px-3 py-2 text-sm ${basis==='trade'?'border-cyan-300/50 text-cyan-200':'border-white/10 text-white/55'}`}>Per trade</button><button onClick={()=>setBasis('day')} className={`rounded-lg border px-3 py-2 text-sm ${basis==='day'?'border-cyan-300/50 text-cyan-200':'border-white/10 text-white/55'}`}>Per day</button></div><div className="grid gap-4 md:grid-cols-2"><div><div className="mb-2 flex gap-2"><button onClick={()=>setRiskMode('dollar')} className={`rounded px-3 py-1 text-xs ${riskMode==='dollar'?'bg-cyan-300/10 text-cyan-200':'text-white/45'}`}>$</button><button onClick={()=>setRiskMode('percent')} className={`rounded px-3 py-1 text-xs ${riskMode==='percent'?'bg-cyan-300/10 text-cyan-200':'text-white/45'}`}>%</button></div>{riskMode==='dollar'?<Field label={basis==='trade'?'Risk per trade':'Daily risk'} value={riskDollar} setValue={setRiskDollar} prefix="$" max={calc.drawdown}/>:<Field label={basis==='trade'?'Risk per trade':'Daily risk'} value={riskPercent} setValue={setRiskPercent} suffix="%" step={0.01} max={100}/>}<p className="mt-2 text-xs text-white/45">{usd.format(calc.risk)} · {(calc.risk/calc.account*100).toFixed(2)}% of account</p></div>{basis==='trade'&&<Field label="Maximum trades per session" value={trades} setValue={setTrades} min={1} max={100}/>}</div></div>:<div className="mt-5 grid gap-4 md:grid-cols-2"><Field label="Target consecutive losing sessions" value={target} setValue={setTarget} min={1} max={10000}/><Field label="Maximum trades per session" value={trades} setValue={setTrades} min={1} max={100} note="Optional: leave at 1 if you only want a daily risk amount."/></div>}</section>
 <section className="rounded-2xl border border-cyan-300/25 bg-gradient-to-br from-violet-500/[0.1] to-cyan-500/[0.05] p-6 text-center">{mode==='risk'?<><p className="font-mono text-xs uppercase tracking-[.18em] text-cyan-200">Account survival</p><p className="mt-3 text-8xl font-bold text-white">{calc.sessions}</p><h2 className="mt-2 text-xl font-semibold text-white">Consecutive losing sessions</h2>{basis==='trade'&&<p className="mt-3 text-sm text-white/60">{calc.losses} consecutive losing trades · {calc.t} trades per session</p>}<div className="mt-6 grid gap-3 sm:grid-cols-3"><Metric label="Maximum drawdown" value={usd.format(calc.drawdown)} tone="text-cyan-200"/><Metric label={basis==='trade'?'Risk per trade':'Daily risk'} value={usd.format(calc.risk)} tone="text-cyan-200"/><Metric label="Risk" value={`${dailyPct.toFixed(2)}% of account`} tone="text-cyan-200"/></div><p className="mt-5 text-sm text-white/60">Formula: {usd.format(calc.drawdown)} ÷ {usd.format(calc.daily)} = {calc.sessions} sessions</p></>:<><p className="font-mono text-xs uppercase tracking-[.18em] text-cyan-200">Survival target</p><p className="mt-3 text-8xl font-bold text-white">{calc.goal}</p><h2 className="mt-2 text-xl font-semibold text-white">Consecutive losing sessions</h2><div className="mt-6 grid gap-3 sm:grid-cols-3"><Metric label="Maximum daily risk" value={usd.format(calc.recommendedDaily)} tone="text-cyan-200"/><Metric label="Maximum risk per trade" value={usd.format(calc.recommendedTrade)} tone="text-cyan-200"/><Metric label="Daily risk capacity" value={`${(calc.recommendedDaily/calc.drawdown*100).toFixed(2)}% of drawdown`} tone="text-cyan-200"/></div><p className="mt-5 text-sm text-white/60">Formula: {usd.format(calc.drawdown)} ÷ {calc.goal} = {usd.format(calc.recommendedDaily)}/day</p></>}</section></div>;
}
