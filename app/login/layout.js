export const metadata = {
  title: 'Sign In — PropLogAI',
  description: 'Sign in to your PropLogAI trading journal.',
  robots: 'noindex',
};
export default function LoginLayout({ children }) {
  return children;
}
